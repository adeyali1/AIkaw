
# Core AI dependencies (Pinned to 2026 Compatibility)
# WhisperX requires torch~=2.8.0 (2.8.x), rejects 2.9+
torch==2.8.0
torchaudio==2.8.0
transformers>=4.48.0
numpy>=2.1.0

# UI (Use modern Gradio 5)
gradio>=5.0.0

# Audio processing & Helpers
# Modern scipy/pandas
scipy>=1.13.0
pandas>=2.2.0
imageio-ffmpeg>=0.5.1

# WhisperX (install from git)
git+https://github.com/m-bain/whisperx.git
