
# Core AI dependencies (Pinned strictly to stop backtracking)
# Python 3.10 compatible versions that work well together
torch==2.3.1
torchaudio==2.3.1
transformers==4.48.0
numpy==1.26.4

# UI (Pinned to stable 4.x to avoid numpy 2 conflicts)
gradio==4.37.2
gradio_client==1.0.2

# Audio processing & Helpers
scipy==1.13.1
pandas==2.2.3
imageio-ffmpeg==0.5.1

# WhisperX (install from git, this one must remain unpinned or precise commit)
git+https://github.com/m-bain/whisperx.git
